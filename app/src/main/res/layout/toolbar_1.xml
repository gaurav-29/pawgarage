<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="54dp"
    android:layout_marginHorizontal="18dp"
    android:gravity="center_vertical">

    <ImageButton
        android:id="@+id/backToolbarOne"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentBottom="true"
        android:backgroundTint="@android:color/transparent"
        android:src="@drawable/ic_back" />

    <TextView
        android:id="@+id/titleToolbarOne"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/backToolbarOne"
        android:layout_alignParentTop="true"
        android:layout_toStartOf="@id/generalImgToolbarOne"
        android:layout_alignParentBottom="true"
        android:fontFamily="@font/poppins_medium"
        android:gravity="center"
        android:paddingTop="4dp"
        android:text="Title"
        android:singleLine="true"
        android:ellipsize="end"
        android:textColor="@color/black"
        android:textSize="20sp" />

    <ImageButton
        android:id="@+id/generalImgToolbarOne"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:src="@drawable/ic_notification_filled"
        android:backgroundTint="@android:color/transparent"
        android:visibility="invisible" />

</RelativeLayout>